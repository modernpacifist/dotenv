#!/bin/env bash
if [[ $1 =~ ^[0-9]{1,5}$ ]];
then
	case $EUID in
		"0") sed -i "18s/.*/export port=$1/" ~/.pentest_values; sed -i "18s/.*/export port=$1/" /home/*/.pentest_values;;
		*) sed -i "18s/.*/export port=$1/" ~/.pentest_values;;
	esac
else
    echo "You should specify correct port number"
    exit 1
fi
